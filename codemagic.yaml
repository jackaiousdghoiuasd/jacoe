workflows:
  run-custom-cli:
    name: Run Custom CLI Script
    environment:
      node: latest
    scripts:
      - name: Execute CLI with config
        script: |
          node $CM_BUILD_DIR/cli.cjs --config $CM_BUILD_DIR/config.json
